<h2><%= l(:label_equipment_new) %></h2>

<%= form_tag(lab_equipment_index_path, method: :post) do %>
  <%= render partial: 'form' %>

  <p class="buttons">
    <%= submit_tag l(:button_create) %>
    <%= link_to l(:button_cancel), "/settings/plugin/redmine_lab_flow?tab=lab_equipment" %>
  </p>
<% end %>
